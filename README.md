trimet-geojson-rest-server
==========================

Creates a simple rest API to respond with individual GeoJSON TriMet routes.

Creates a schedule to update every single night so that you always have update to date routes.